{
  "name": "blink-reading",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check app.js && node --check engine.js && node --check storage.js && node --check content.js"
  },
  "engines": { "node": ">=20" }
}
